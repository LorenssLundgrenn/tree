recursively lists every file of a given directory
you can also search for files by keyword

required args:
  first argument must specify directory path
optional args:
  second argument specifies search keyword
